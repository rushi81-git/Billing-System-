# Leave VITE_API_URL blank to use auto-detection (recommended).
# Auto-detection works for both localhost AND network devices (phones/tablets).
#
# Only set this manually if auto-detection fails:
# VITE_API_URL=http://192.168.1.5:5000/api   ← replace with your PC's IP
#
VITE_API_URL=
VITE_APP_NAME=Smart POS
